## 6.15号13.39

首先分析我的自动化脚本，接下来我需要你进行一些优化：

1. 把所有配置文件相关的参数，例如：Trae CN软件地址，Anaconda的pyhon环境，Deepseek API相关的配置都放在config.json文件中，方便我把代码发给其他人的时候他们配置参数。
2. 现在执行github上传代码的时候，我发现一个问题，就是同Batch（也就是假设我打开了8个窗口），出现一个问题就是，第一个窗口的commitid可能与第二个窗口的commitid是一样的，他俩打开的不是同一个文件，commitid怎么可能是一样的，需要你排查这个问题。



## 6.15号13.55

现在我想让你加一个逻辑，就是我可以控制一些参数：

1. 可以控制第二轮及之后task_type为Bug修复或者Feature迭代的概率，写在config.json中（我可以手动设置），但是有一点需要注意，第二轮及之后应该先根据概率去生成task_type，然后根据task_type去生成对应轮次的prompt,不要出现task_type为Bug修复，但是提示词想让模型执行的是Feature。
2. 加一个参数，可以控制最后一轮产物满意的概率，我手动设置，在config.json中配置。



我现在想把代码传到github上，到另外一个仓库（主要是传项目代码，方便与其他人同步），但是我现在代码中运行过程中又把生成的项目传到https://github.com/ZSL-haut/Solo这个地址了，有没有什么办法？先给我说思路，然后把操作流程写一个.md文件到/doc文件夹下，主要涉及：

1. github仓库如何连接，远程仓库查看（主要看仓库连接的是否正确）
2. 仓库切换，因为项目运行连接的是一个远程仓库，代码更新连接的是另外一个仓库。
3. 如果上传指定的文件，或者拉取指定文件（主要是我不想把我config.json中的APIKey传上去，他人拉取的时候能看到，我不想泄露）。

